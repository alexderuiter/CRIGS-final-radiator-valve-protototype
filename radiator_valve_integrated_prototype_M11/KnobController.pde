class KnobController{

  
  KnobController(){
    
  }
  
  void determineDesiredTemperature() {
    // get Absolute angle
    
    // map absolute angle to temperature
  }
  
  void determineResistance(){
    // if (previouselyDesiredTemperatureValid || sustainabilityTemperature < desiredTemperature) add extra resistance (always the same value)
    // else make resistance normal resistance
  }
  
  boolean previouselyDesiredTemperatureValid(){
    // get last setting finishing timestamp
    
    // get current timestamp
    
    // if (current timestamp < last setting finishing timestamp) return true
    // else return false
  }
  
}